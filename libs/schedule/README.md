# @daffodil/schedule
`@daffodil/schedule` contains utilities useful for tracking current schedules.

## Installation
To install `@daffodil/schedule`, use the following commands in your terminal.

Install with npm:
```bash
npm install @daffodil/schedule --save
```

Install with yarn:

```bash
yarn add @daffodil/schedule
```
